import { Router } from "express";
import { asyncHandler, badRequest } from "../middleware/errorHandler";
import { validateBody } from "../middleware/validate";
import { archiveListing, createListing, getListingDetail, getRecommendedMatchesForListing, getSimilarListings, listListings, updateListing } from "../services/listings.service";
import type { ListingMode, ListingStatus, ListingType, SkillCategory } from "../types/domain";
import { createListingSchema, updateListingSchema } from "../validation/schemas";
export const listingsRouter = Router();
listingsRouter.get("/listings", asyncHandler(async (req, res) => { res.json({ data: await listListings({ type: req.query.type ? (String(req.query.type) as ListingType) : undefined, q: req.query.q ? String(req.query.q) : undefined, category: req.query.category ? (String(req.query.category) as SkillCategory) : undefined, city: req.query.city ? String(req.query.city) : undefined, canton: req.query.canton ? String(req.query.canton) : undefined, language: req.query.language ? String(req.query.language) : undefined, mode: req.query.mode ? (String(req.query.mode) as ListingMode) : undefined, duration: req.query.duration ? Number(req.query.duration) : undefined, beginnerFriendly: req.query.beginnerFriendly ? String(req.query.beginnerFriendly) === "true" : undefined, recommendedFor: req.query.recommendedFor ? String(req.query.recommendedFor) : undefined, status: req.query.status ? (String(req.query.status) as ListingStatus) : undefined, limit: req.query.limit ? Number(req.query.limit) : undefined, sort: req.query.sort ? (String(req.query.sort) as "recommended" | "newest" | "popular") : undefined, userId: req.query.userId ? String(req.query.userId) : undefined }) }); }));
listingsRouter.get("/listings/:listingId", asyncHandler(async (req, res) => { res.json({ data: await getListingDetail(req.params.listingId, req.query.userId ? String(req.query.userId) : undefined) }); }));
listingsRouter.post("/listings", validateBody(createListingSchema), asyncHandler(async (req, res) => { res.status(201).json({ data: await createListing(req.body, req.demoContext) }); }));
listingsRouter.patch("/listings/:listingId", validateBody(updateListingSchema), asyncHandler(async (req, res) => { res.json({ data: await updateListing(req.params.listingId, req.body) }); }));
listingsRouter.patch("/listings/:listingId/archive", asyncHandler(async (req, res) => { res.json({ data: await archiveListing(req.params.listingId) }); }));
listingsRouter.get("/listings/:listingId/recommended-matches", asyncHandler(async (req, res) => { const userId = String(req.query.userId ?? req.demoContext.actorUserId ?? ""); if (!userId) throw badRequest("userId query parameter or X-Actor-User-Id header is required"); res.json({ data: await getRecommendedMatchesForListing(req.params.listingId, userId, req.query.limit ? Number(req.query.limit) : 3) }); }));
listingsRouter.get("/listings/:listingId/similar", asyncHandler(async (req, res) => { res.json({ data: await getSimilarListings(req.params.listingId, req.query.limit ? Number(req.query.limit) : 4) }); }));
